import type { TickContext } from './context'

// v0.17 §5.4: UnaffiliatedPersonSystem (yearly, Jan)
// Stage A skeleton — identity. Full implementation deferred to Stage B.
export function runUnaffiliatedPersonSystem(ctx: TickContext): TickContext {
  return ctx
}
