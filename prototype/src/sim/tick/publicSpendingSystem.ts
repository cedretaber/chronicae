import type { TickContext } from './context'

// v0.27: development direct addition removed. Land development is now handled via develop_holding Projects.
export function runPublicSpendingSystem(ctx: TickContext): TickContext {
  return ctx
}
