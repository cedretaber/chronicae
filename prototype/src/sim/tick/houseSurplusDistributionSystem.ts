import type { TickContext } from './context'

// v0.17 §10: HouseSurplusDistributionSystem
// Stage A skeleton — identity. Full implementation deferred to Stage B.
export function runHouseSurplusDistributionSystem(ctx: TickContext): TickContext {
  return ctx
}
