import type { TickContext } from './context'

// v0.17 §6.5: OfficeTermSystem (yearly, Jan)
// Stage A skeleton — identity. Full implementation deferred to Stage B.
export function runOfficeTermSystem(ctx: TickContext): TickContext {
  return ctx
}
