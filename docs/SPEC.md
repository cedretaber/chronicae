# Chronicae プロトタイプ仕様書

最終更新: 2026-06-27 (v0.63 POP 勤務先別分離 — `PopGroup.employed: boolean` を `PopGroup.employerId: WorkplaceRef | null` に置換し、POP を勤務先 (不動産/施設/商会店舗) ごとに分離。**WorkplaceRef** tagged union (`asset`/`improvement`/`merchant`) 導入。merge key を `(holdingId, popType, employerId)` に変更。**雇用リバランス** を per-employer 二相アルゴリズムに改修。**賃金** は employer の POP に直接 mint、**階層別勾配** (`wageStratumMultiplier` lower=1.0/middle=1.5) 導入。**upper 配当** を holding 収入 (landRevenueSystem) に移設。**Worldgen** で employer 別 PopGroup 生成。**Lifecycle**: 施設破壊・商会施設削除時の POP 失業化 + reconciliation safety net。**UI**: 就労先リンク・POP カード共通化・Holding POP 概要明確化。150年×4seed integrity green。§3.2 / §5.4 / §9。)

v0.62 (v0.62 Chronicle 外部化 — `WorldState` から `chronicleEntries` / `chronicleIndex` / `nextChronicleEntryId` を完全に除去し、外部ストレージへ移行。ブラウザ preset=small で30年実行時に OOM が発生していた原因（state の65%を占める Chronicle の無制限蓄積）を解消。`ChronicleWriter` interface を sim 層に定義し `TickInput` に DI 注入。CLI は JSONL ファイルに同期書き出し（`--chronicle-dir` / `--chronicle-tag` / `--no-chronicle`）、Browser は IndexedDB にバッチ書き出し（300ms タイマー flush・worldgen 時 clear）。UI の chronicle 表示は `ChronicleReader` interface 経由の非同期取得（IndexedDB の entityRefs ベース multiEntry index）に移行。`maxChronicleEvents` config 削除、integrity check の chronicle 双方向検査削除。sim の決定性に影響なし（RNG 非消費の純粋 projection・bit-identical 確認済み）。150年×4seed integrity green。§3.14 / §4.11 / §6.35 / §6.62 / §9。)

v0.61 (v0.61 商会・交易システム — StateRegion 間の資源流通を担う **商会（MerchantCompany）と交易路（TradeRoute）** を導入。**交易路修正**: 旧式の raw targetImportDemand（大半の route が搬送量ゼロ）を廃止し、前月 **smoothedPrice**（EWMA）の価格差ベースに転換。計算は純関数 `computeExpectedRouteEconomics` に集約。固定維持費を {1:1,...} → {1:0.15,...} に引き下げ＋従量維持費を追加。Accounting は planning price（前月 smoothedPrice）基準で profit を計算（自己注入利益消失を回避）。候補評価を expectedProfit ベースに、HQ 成長を支店建設より前倒し。150年×2seed probe: 64 active routes（57 profitable・旧式では44本がゼロ）、17資源が交易、route level L4 まで成長、閉鎖振動なし。commerce revenue（mint）が route profit を上回る構造は既知（balance-defer）。`MerchantCompanyStatus.bankrupt` は v0.61 現在未使用（予約枠）。UI: 市場パネルを輸入/輸出分割・商会詳細に active Project 一覧・i18n 補完。詳細は §6.71。)

v0.60 (v0.60 Project 資金集めフェーズ改修 — Project の開始ハードルを下げ、開始後に複数回の「資金集めラウンド」をステークホルダーから決定的に集めて長期だが確実に完遂させる。対象は budget を持つ建設・取得系 5 種（develop_holding / develop_real_estate / acquire_real_estate / upgrade_owned_real_estate / handle_crisis）。**secure_budget の軽量化**: 旧「owner が required 全額を一括先払い・不足で stall」を、`projectInitialReserveFraction`(0.3) 分だけ owner stock から確保し stock 不足でもハード失敗せず開始する方式へ（不足分は後続ラウンドで集める）。**raise_funds back-edge ステージ**: 既存 linear stage 機構に final（execute_project/mitigate）の後ろへ `raise_funds`(immediate) を追加。`projectMaintenanceSystem` は budget 枯渇（remaining≤0 かつ progress<target）時、即失敗でなく `currentStageKey='raise_funds'` へ進捗を保ったまま遷移し、次 tick の `projectStageSystem.resolveRaiseFunds` がステークホルダーから決定的に pledge を集めて budget へ上乗せ・deadline を `projectFundingDeadlineExtensionWeeks`(48) 延長し final へ復帰する。`getNextProjectStageKey` は raise_funds を linear next に出さない（back-edge 専用）。**ステークホルダープール**（`getProjectFundingStakeholders`・ID 昇順・重複排除・insider 優先）: insider = owner / creator / supervisor / 現地代官（高率・能力非依存 `insiderMaxContributionFraction` 0.5）、external = owner が polity なら関連 House/Person（commonwealth は republic candidate union で空にならない）＋ローカル POP。acquire_real_estate は私的取得のため POP・関連 Polity を含めず owner House＋メンバーのみ。**pledge 算出**（`computeContributorPledge`・RNG 不使用）: insider は stock×insiderFraction、external は stock×`fundraisingMaxContributionFractionByContributorKind`×能力係数×関係係数。能力係数 = supervisor の diplomacy ロール score を `fundraisingAbilityExponent`(1.4) で非線形スケール（人物中心史観：有能な調達者ほど多く集める）。関係係数 = Person/POP の supervisor への attitude（affection0.6+respect0.4）。POP は生活費 horizon（`popContributionHorizonMonths` 3ヶ月分・需要金額=Σ buyOrders×price）を超える余剰のみ拠出。**保存則**: 拠出は出し手 stock（treasury/wealth/money）を実減算し budget へ加算、各 stock≥0 を clamp。raised は requiredRemaining に比例 cap し allocated が required を超えない（acquire の seller 決済が budget.allocated に依拠するため over-collection=貨幣創造を防ぐ・acquire 完了時 seller 支払いを salePrice から budget.allocated へ変更し funded 量と一致させる）。**終了保証 2 本**: ラウンド最小回収（`projectFundingRoundMinCollectionFraction` 0.15）未満で `funding_failed`／最大ラウンド（`projectMaxFundingRounds` 3）超で budget_exhausted。**イベント/Chronicle**: ラウンド成立で `PROJECT_FUNDED`（raised>0 のみ）、建設・取得 5 種の完成で `PROJECT_BUILT`（development・「監督者＋主要出資者(`majorContributors` 累積上位)による建造」を durable history に記録）。config 既定値は控えめ＝balance-defer。150年×4seed integrity green・determinism bit-identical 維持。§6.38 / §6.40 / §9。**balance-defer**: 失敗時 refund が owner のみ＝external 拠出を owner が回収する fairness regression（拠出見返り未実装ゆえ容認・将来 funder rewards で対処）。§6.38。

v0.59 (v0.59 マップ改善 ＋ 地形特性 — worldgen に最低保証を追加: **全 5 地形を World 単位で保証**（鉱業/林業が成立しない世界を排除。低確率地形が欠けたら冗長地形を決定的に上書き）/ **全 state に最低 1 city**（world min2 を state 単位保証へ格上げ・全 preset）/ **全 Province に manor≥2**（city Province=2 manor+1 city=3 holding・全 preset）/ **農園を全地形で建設可**（mountains 追加・容量倍率 0.25＝狭く雇用少。World 地形保証の安全弁を兼ね、山岳偏重 state でも食料 >0）。**地形特性 (TerrainTrait)** を導入: `Province.traits`（config 駆動 `terrainTraitDefinitions`・名前付き・地形/地物相関・複数可・確率付与・決定的 `assignTerrainTraits`）。効果は 2 種——**産出 trait**（input を持たない raw 資源の per-output 倍率。`resourceProductionSelectors` の output 確定点で乗算・input 据え置き＝純生産性。fertile_land→grain×1.3 等。`potentialOutputs→sellOrders→扶養力` に比例反映）と **広闊な地形 trait**（`computeSlotCapacity` で不動産スロット上限 +1＝雇用を「施設の数」で表現）。付与密度は `terrainTraitDensityMultiplier`、地形割合は既存 `provinceTerrainWeights`、いずれも worldgen に config を threading して `--config` で調整可。**`landQuality` を廃止**（無名のランダム容量ジッタ。容量乗数から除去＝実質 1.0・平均不変。holding 単位の変動表現は「広闊な地形」trait に移譲。worldgen 生成・goalSelector tiebreaker・integrity 検査・UI 品質表示を全撤去、UI は trait 表示へ置換）。**人口変動の可視化** を追加: read-model `WorldState.monthlyPopChange`（先月＝直近 4 週の純人口変動を **自然増減と移住に分解**して holding 単位・POP グループ単位で保持。`PopSystem` が月初リセット生成し自然増減を、`CrisisSystem` が飢饉/疫病/戦災死を `reduceHoldingPopSizeProportionalMut` の `onReduce` callback で、`PopMigrationSystem` が holding 間移動を、それぞれ in-place 累積。RNG 不使用で dump-world bit-identical 維持）。Holding/POP detail に「人口変動 (先月)」セクション（純変動＋自然増減＋移住流入/流出。holding 合計 = natural + migrationIn − migrationOut で一致）。**POP 移動 cap の per-source 再設計**（追補）: 観察された「下層は求人枠が余るのに人手不足／中産は失業のまま降格しない」の構造修正。(1) 転職・移住の cap を holding 共有予算から **per-source（source.size × レート）・移動先非依存** へ（小 holding の移動凍結を解消、holding-cap config 6 個を削除）。(2) **移動と雇用を分離**（移動先では失業着地、雇用は rebalance が確定）。(3) **昇格だけ移動先の実効職枠を gate**（降格・移住は移動先非依存）。(4) 判断系（shortage/空き枠/移住 vacancy）を **maxRatio 後の実効容量** で評価（埋まらない熟練職枠を需要に計上しない・`getHoldingPopType(All)EffectiveCapacit*`）。数値据え置き・150年×4seed integrity green・determinism 維持。§3.1 / §6.3b / §6.3c / §7.1 / §9 / §14.1.5。**ボトルネック資源への生産性投資**（追補③）: develop_holding が capacity（職枠）増設ばかり建て productivity 改良（灌漑等）を建てず食料生産性が経年で伸びない問題の構造修正。`buildProjectFieldsForAim` の決定木先頭に「**ボトルネック資源を生産する holding は capacity でなく production_quality 改良を優先**」分岐を追加（汎用: 資源→改良の対応は `REAL_ESTATE_PRODUCTION_FACILITY_MODIFIERS` 逆引き）。**食料は市場が自己均衡し shortage が見えない**ため `getResourceBottleneckSeverity = max(市場 shortageSeverity, 食料 pressure)` とし、食料資源は人口圧（state人口/食料CC ≥ `foodBottleneckPressureThreshold`）で検出。`hasEmploymentSlack` では gate せずボトルネック検出自体をトリガに（食料束縛 holding は全員就業でも slack 無しになりうる）。**灌漑を全地形建設可に degate** し、改良コストの地形/地物割引テーブル（`holdingImprovement{Terrain,Feature}CostMultiplier`・灌漑は wetlands/河川/湖沼で割引・海なし）を新設。`selectors/productivityBottleneckSelectors.ts` 新設。**到達性修正**: `develop_owned_holding` aim 生成条件（従来 devDeficit/capacityPressure/employmentSlack）の最後の disjunct にボトルネック検出を短絡追加し、食料束縛で全員就業の holding（3条件が全偽になるデッドゾーン）でも改良 aim が生成されるようにした。RNG 不使用・150年×4seed integrity green・determinism 維持。§6.40（§6.55 buildProjectFieldsForAim）。 **POP 需要充足率の内訳表示**（追補④）: 食料不足で人口が減る世界を POP 側から診断できるよう、経済 tick が内部計算しながら畳んで捨てていた **NeedCategory 別充足度**（= `affordByTier[cat.tier] × カテゴリ market-fill ×100`）を `PopGroup.categorySatisfaction`（表示専用キャッシュ・`Partial<Record<NeedCategory,number>>`・desire を持つカテゴリのみ key を持ち「需要なし」と「0%」を区別）に保存し、POP 詳細で必需品/日用品/贅沢品にグループ化してバー表示（赤<40/橙<70/緑）。`needSatisfaction`/`money` はビット同一（243 POP で baseline と完全一致）、merge/worldgen/integrity 非追従・挙動不変。§6.3c.5。 **自己レビュー反映**（マージ前レビューで発見・修正）: (1) worldgen 全体に config を threading し `--config` の上書きが初期世界生成（性比・地形・地物・初期人口・容量等）にも反映されるよう統一（旧「worldgen --config 不感」を解消・既定は defaultConfig で未指定時不変）。(2) 全地形保証の terrain 上書き後に feature を新地形で再ロール（feature 確率は terrain 依存のため・上書き世界のみ RNG 追加消費）。(3) 未使用化した `reduceProvincePopSizeProportional` を削除。(4) 地形特性 lookup を config 単位 WeakMap でメモ化（hot path の線形 find を O(1) 化）。150年×4seed integrity green・determinism 維持。)

v0.58 (v0.58 POP 貨幣経済 — POP の財産を 0..100 `wealth` 指数から具体的な **`money` 残高(stock)** へ移行。source/sink モデル（賃金=mint・消費=burn、抽象市場は非保存のまま流用）。**賃金 mint**: resourceEconomySystem 最終 pass で `wageShare = max(0,netRevenue)×wageShareOfNetRevenue`(0.3) を雇用 PopType へ役割重み付き配分（`computeAssetPopTypeShares × wageRoleWeightByRole`、carve==mint）。LandRevenue は `positiveNet = max(0,netRevenue−wageShare)` で控除。**予算制約消費**: start-of-tick money を essential→ordinary→luxury の tier 優先で充当（full desired×afford、`purchasingPowerFactor` 撤去）。**needSatisfaction**(0..100 intensive) = `Σ tier重み×(afford×marketFill)` 平滑化で unrest/成長/mobility を駆動（0..100 `wealth` 退役）。money は extensive で人口移動/merge は per-capita 保存（移動=比例・merge=sum・死亡=比例burn・出生=据置）。mobility gate は `computePopTypeMoneyQuantiles`（per-capita money）。詳細は §6.3c.5。**v0.58 balance 校正**: 初版（wageRate 0.3・essentialScale 1.0）は賃金が essential 消費の ~15% しか賄えず全 POP が貧困床（needSatisfaction 中央値 ~5）に貼り付く過少消費だった。**essential desire 半減（`popEssentialNeedScale` 0.5）＋ 賃金率 0.3→0.5** で needSatisfaction 中央値 ~5→~45・人口/雇用回復・不穏度 calm。さらに上流(nobles/patricians)は「給料」でなく**配当**（`upperDividendShareOfNetRevenue` 0.03＝holding 純収益の固定割合を**雇用枠の upper POP のみ**へ、失業 upper は没落→降格）で upper needSatisfaction 0→~45・富の勾配が出現。**施設(improvement)労働者**(兵士・事務官等 lower/middle)は holding 収入から生産者と同 per-capita の俸給を別途確保（`facCap/prodCap` で stratum 単位の総額算出）。**v0.58 で受取人を是正**: 旧実装は施設人件費を生産者 POP に上乗せ mint しており、生産 asset に登場しない施設専用 popType(soldiers/bureaucrats/merchants 等。soldiers は improvement 専用)が収入ゼロ→needSat 0 だった。これを施設労働者本人へ付け替え（総額・カバレッジ不変＝owner 影響ほぼ不変 wageShare 62.7%→63.6%、施設専用 popType の購買力改善 例 soldiers pcMoney 3.5→16.6、150年×4seed green・determinism 維持）。lower/middle の POP.money 死蔵（mint > goods・sink は消費のみ）対策に **POP 資産課税**（`popWealthTaxRate` 0.03＝per-capita floor 超過分に課税、land 税と同じ holding 収入に合流＝代官徴収・手数料・chain 共通、納税額≠徴税額の差は徴税効率ロスとして消滅、floor 以下非課税・upper 除外）を導入し死蔵を drain。富の蓄積プールは lower/middle=drain 済 / upper=将来 Project 出資 / Person.wealth(代官)=出世ルートの望ましい蓄財 / House・Treasury=流動的、と整理。balance-defer（rate/floor 微調整・国別課税方式は将来「国の制度」）、§6.3c.5 / §13。)

v0.57 (v0.57 POP 雇用の職能細分化 ＋ v0.57.1 移動の Class 統一 — 雇用枠を **PopStratum 単位から施設駆動の PopType 単位ハード枠**へ再設計（`RealEstateEmploymentSlot` / `ImprovementEmploymentSlot` を PopType キー化、熟練職の同数上限 `maxRatioTo`、EmploymentRebalance を PopType 単位化、`computeHoldingPopTypeDemand` の desired は施設駆動容量に）。職能効果: 親方/自作農 `directOutputPower 1.5`・書記 `throughputBonus 0.5`（生産）、治安〔兵士・家士〕で unrest 低減・維持〔労働者〕で施設 condition 減衰緩和（管理職能は循環回避のため将来送り、§6.x.v0.57）。**v0.57.1**: POP の移動（転職・移住・昇格・降格）の判断を **Class（PopType）単位に統一**（wealth 相対 gate の母集団を同職能へ・idealShare/currentShare を holding 全体正規化・`classifyMobilityKind` をエッジ所属判定へ・`computeStratumWealthQuantiles`→`computePopTypeWealthQuantiles`）、worldgen 初期 POP を施設駆動 PopType 枠に比例 seed（固定職能分布 `WORLDGEN_POP_TYPE_DISTRIBUTION` を廃止し初期の構造的失業を解消）。詳細は §6.x.v0.57 / §6.3b)

v0.56 (POP 転職・移住システム — v0.55 の PopType / PopStratum / 資源経済 / recipe 労働需要を前提に、POP の自律的な **転職**（同一 holding 内の職種/階層変更 = lateral/promotion/demotion）と **移住**（同一 StateRegion 内の holding 間移動）を追加。EmploymentRebalance → **PopJobChange → PopMigration → PopEmploymentNormalize** → ResourceEconomy の月次連鎖（§6.3b）。共通 mutation `movePopSizeToKeyMut`（size 移送・人口加重 merge・人口保存）、read-model `computeHoldingPopTypeDemand`（施設駆動の PopType 雇用容量 → shortage/surplus、idealShare は holding 全体正規化〔v0.57.1〕）、`computePopTypeWealthQuantiles`（StateRegion × PopType・size 加重の wealth 分位〔v0.57.1: 旧 `computeStratumWealthQuantiles` を職能単位へ〕）。**転職**は候補優先度ループ・holding 人口比 cap（C2）・**promotion/demotion は相対 wealth 分位 gate**（C3。絶対閾値は wealth≈0 で不発）・lateral の capacity gate は employed 増の move のみ（C1）。**移住**は migration pressure 閾値・opportunity score（vacancy/構成ミスマッチ B2/wealth/unrest − cross-polity penalty）・人口比 outflow/inflow cap。RNG 不使用で dump-world bit-identical 維持。read-model `WorldState.monthlyPopMobility`（latest 毎月上書き、UI は Holding=移住 / POP=階層移動・転職 に drill-down、topMovements は store-all 相当）。EmploymentRebalance の再就業は v0.57 で PopType 単位ハード枠へ再設計され、v0.56 の「stratum 跨ぎ shortage 優先」demand-aware Phase2 を置換した（§6.x.v0.57）。詳細は draft `spec-v056-update.md`(r2) / §6.3b)

v0.55 (商品経済への大規模再編 — v0.54 の 3 資源を **21 ResourceKind** へ拡張。**NeedCategory / InputCategory** 導入（POP 需要・recipe 投入をカテゴリ化、カテゴリ内の resource 選択は utilityPerMoney 比率配分で加工品死蔵を回避）。**RealEstateKind を farm/mountain/woodland/workshop** へ再編（一次産業=manor・工房=city）。**ProductionRecipe を 23 種**に細分化（複数 output・複数 input・規模の経済）。**PopType（12 職能）/ PopStratum（旧 PopClass の値移行 lower/middle/upper）** 導入、merge key に popType 追加、RealEstateAsset は複数 stratum を雇用可。需要を PopType 別 `PopNeedProfile` ＋ NeedTier 購買力曲線で定義。**market 清算を DAG 依存順 1-pass** へ一般化（resource level 静的計算・cycle は integrity error・複数 input は Liebig 最小律 + floor 付き inputShortageModifier §6.3c.2）。**laborTypeFulfillmentModifier**（PopType 理想構成への soft modifier、floor 0.70）。**RecipeSwitchSystem (§6.3d)**（四半期・best-improvement・smoothedPrice で recipeSlots 自動入れ替え）。**建設・修繕 Project の建築資材需要**（timber/stone/tools を市場注入、budget を週次・価格ベースで消費）。飢饉=物理 pressure 駆動の急性餓死・干魃=食料生産被害へ整合 (§6.27 / draft §C)。**未実装(deferred)**: 追加予算 top-up・動的 deadline 延長・ProjectMaterialPurchaseSnapshot・一部 integrity 静的検査・市場 digest カテゴリ集約 UI (draft §20–§25)。詳細は draft `spec-v055-update.md`)

v0.54 (資源経済導入 — POP 直接 production を廃止し ResourceEconomySystem (§6.3c) を追加。RealEstateAsset+POP 労働→資源 (food/raw_materials/processed_goods) 生産→StateRegion 市場で売却→money revenue。recipeSlots / ProductionRecipe / MarketResourcePriceState / 月次 HoldingResourceRevenueSnapshot。LandRevenueSystem は資源 snapshot を source に owner income/holding due 分割 (realEstateHoldingDueRate)・bailiff・chain 分配へ再構成。食料/加工品の充足率・価格を POP wealth/unrest に反映。obligation accrual を月額化。**market-clearing rewrite (§6.3c.1): Victoria 3 型の抽象市場へ — sell orders 全量 revenue 化・buy orders 全量 cost 評価・imbalance ベース価格 (marketPriceSwing、資源別 min/max/elasticity 廃止)・fulfillmentRatio / shortage / shortageSeverity による penalty。供給過多は安値で全量売却、供給不足は高価格+shortage penalty。marketValueDelta は市場抽象化で非保存だが LandRevenue 分配の保存則は不変**)

v0.53 (押領・土地契約不履行・時効 — RealEstateSeizure / LandContractDefault 導入、seize/withhold/enforce Project、Pressure 経由の対応、20年時効による既成事実化 (spliceOutClaimantContract)、revolt_seizure 廃止→nominal occupation contract + revolt_independence default、非 root tax-0 invariant、obligationConsistency/Accrual/prescription/cleanupTerminalObligations system 追加)

---

## 目次

| # | 章 | ファイル |
|---|---|---|
| 1 | [概要](spec/01-overview.md) | `spec/01-overview.md` |
| 2 | [技術構成](spec/02-tech-stack.md) | `spec/02-tech-stack.md` |
| 3 | [エンティティ型](spec/03-entities.md) | `spec/03-entities.md` |
| 4 | [セレクター](spec/04-selectors.md) | `spec/04-selectors.md` |
| 5 | [Tick システム順序](spec/05-tick-order.md) | `spec/05-tick-order.md` |
| 6 | [各システムの仕様](spec/06-systems.md) | `spec/06-systems.md` |
| 7 | [Worldgen 初期化](spec/07-worldgen.md) | `spec/07-worldgen.md` |
| 8 | [イベント型一覧](spec/08-event-types.md) | `spec/08-event-types.md` |
| 9 | [SimulationConfig デフォルト値](spec/09-config.md) | `spec/09-config.md` |
| 10 | [人物能力効果](spec/10-ability-effects.md) | `spec/10-ability-effects.md` |
| 11 | [UI 構成](spec/11-ui.md) | `spec/11-ui.md` |
| 12 | [アーキテクチャ原則](spec/12-architecture.md) | `spec/12-architecture.md` |
| 13 | [今後の課題（未実装）](spec/13-future.md) | `spec/13-future.md` |
| 14 | [ゲームバランス観察記録](spec/14-balance.md) | `spec/14-balance.md` |
